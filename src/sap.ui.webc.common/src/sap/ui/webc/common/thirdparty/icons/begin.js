sap.ui.define(['sap/ui/webc/common/thirdparty/base/config/Theme', './v5/begin', './v4/begin'], function (Theme, begin$2, begin$1) { 'use strict';

	const pathData = Theme.isTheme("sap_horizon") ? begin$1 : begin$2;
	var begin = { pathData };

	return begin;

});
