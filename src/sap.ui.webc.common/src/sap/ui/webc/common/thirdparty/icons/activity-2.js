sap.ui.define(['sap/ui/webc/common/thirdparty/base/config/Theme', './v5/activity-2', './v4/activity-2'], function (Theme, activity2$2, activity2$1) { 'use strict';

	const pathData = Theme.isThemeFamily("sap_horizon") ? activity2$1 : activity2$2;
	var activity2 = { pathData };

	return activity2;

});
