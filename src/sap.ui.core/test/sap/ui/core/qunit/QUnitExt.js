/*eslint no-unused-vars:0 */// eslint complains about unused global, but it's used elsewhere
/* eslint-disable no-implicit-globals */
function assert() {
	// just define a global assert function
	"use strict";
}