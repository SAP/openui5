sap.ui.define(['sap/ui/core/Core', 'sap/ui/core/library'], function(Core, coreLib) {
	"use strict";
	sap.ui.getCore().initLibrary({
		name: 'testlibs.themeParameters.lib8',
		version: '1.0.0',
		apiVersion:2,
		dependencies: [
		]
	});
	return testlibs.themeParameters.lib8; // eslint-disable-line no-undef
});
