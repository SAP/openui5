sap.ui.define(['sap/ui/core/Core', 'sap/ui/core/library'], function(Core, coreLib) {
	"use strict";
	sap.ui.getCore().initLibrary({
		name: 'testlibs.themeParameters.lib10',
		version: '1.0.0',
		apiVersion:2,
		dependencies: [
		]
	});
	return testlibs.themeParameters.lib10; // eslint-disable-line no-undef
});
