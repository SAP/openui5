/*!
 * ${copyright}
 */
sap.ui.define([
], function(
) {
	"use strict";

    return {
        run: function(config) {
            //config.set("sapUiLibs", ["sap.ui.core", "sap.m"]);
            return Promise.resolve();
        }
    };
});