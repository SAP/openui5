/*!
 * ${copyright}
 */

// Provides the Design Time Metadata for the sap.m.ActionSheet control
sap.ui.define([],
	function () {
		"use strict";

		return {
			aggregations: {
				buttons: {
					domRef: ":sap-domref",
					actions: {
						move: "moveControls"
					}
				}
			}
		};

	});
