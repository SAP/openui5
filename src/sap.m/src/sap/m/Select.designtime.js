/*!
 * ${copyright}
 */

// Provides the Design Time Metadata for the sap.m.Select control
sap.ui.define([],
	function() {
	"use strict";

	return {
		aggregations : {
			items : {
				domRef : ":sap-domref"
			}
		}
	};

}, /* bExport= */ false);
