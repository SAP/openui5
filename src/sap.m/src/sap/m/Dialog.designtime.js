/*!
 * ${copyright}
 */

// Provides the Design Time Metadata for the sap.m.DialogType control
sap.ui.define([],
	function () {
		"use strict";

		return {
			aggregations: {
				content : {
					domRef : "> .sapMDialogSection"
				}
			}
		};

	}, /* bExport= */ false);
