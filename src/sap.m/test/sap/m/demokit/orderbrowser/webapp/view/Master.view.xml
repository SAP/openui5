<mvc:View
	controllerName="sap.ui.demo.orderbrowser.controller.Master"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:semantic="sap.m.semantic">

	<semantic:MasterPage
		id="page"
		title="{masterView>/title}"
		navButtonPress="onNavBack"
		showNavButton="true">
		<semantic:subHeader>
			<Bar id="headerBar">
				<contentMiddle>
					<SearchField
						id="searchField"
						showRefreshButton="{= !${device>/support/touch} }"
						tooltip="{i18n>masterSearchTooltip}"
						width="100%"
						search="onSearch">
					</SearchField>
				</contentMiddle>
			</Bar>
		</semantic:subHeader>

		<semantic:content>
			<PullToRefresh
				id="pullToRefresh"
				visible="{device>/support/touch}"
				refresh="onRefresh" />
			<!-- For client side filtering add this to the items attribute:  parameters: {operationMode: 'Client'}}" -->
			<List
				id="list"
				items="{
					path: '/Orders',
					parameters: {expand: 'Customer'},
					sorter: {
						path: 'OrderID',
						descending: false
						},
					groupHeaderFactory: '.createGroupHeader'
				}"
				busyIndicatorDelay="{masterView>/delay}"
				noDataText="{masterView>/noDataText}"
				mode="{= ${device>/system/phone} ? 'None' : 'SingleSelectMaster'}"
				growing="true"
				growingScrollToLoad="true"
				updateFinished="onUpdateFinished"
				selectionChange="onSelectionChange">
				<infoToolbar>
					<Toolbar
						active="true"
						id="filterBar"
						visible="{masterView>/isFilterBarVisible}"
						press="onOpenViewSettings">
						<Title
							id="filterBarLabel"
							text="{masterView>/filterBarLabel}" />
					</Toolbar>
				</infoToolbar>
				<items>
					<ObjectListItem
						type="{= ${device>/system/phone} ? 'Active' : 'Inactive'}"
						press="onSelectionChange"
						title="{
							path: 'OrderID',
							formatter: '.formatter.masterItemTitle'
						}"
						number="{
							path: 'OrderDate',
							type: 'sap.ui.model.type.Date',
							formatOptions: { style: 'short' }
						}">
						<attributes>
							<ObjectAttribute text="{i18n>masterItemCustomer}: {Customer/CompanyName}" />
							<ObjectAttribute text="{i18n>masterItemShipped}: {
								path: 'ShippedDate',
								type: 'sap.ui.model.type.Date',
								formatOptions: { style: 'medium' }
							}" />
						</attributes>
					</ObjectListItem>
				</items>
			</List>
		</semantic:content>

		<semantic:sort>
			<semantic:SortSelect change="onSort">
				<core:Item key="OrderID" text="{i18n>masterSortOrderNumber}" />
				<core:Item key="Customer/CompanyName" text="{i18n>masterSortCustomer}" />
				<core:Item key="OrderDate" text="{i18n>masterSortOrderDate}" />
				<core:Item key="ShippedDate" text="{i18n>masterSortShippedDate}" />
			</semantic:SortSelect>
		</semantic:sort>
		<semantic:filter>
			<semantic:FilterAction press="onFilter"/>
		</semantic:filter>
		<semantic:group>
			<semantic:GroupSelect change="onGroup">
				<core:Item key="NO_GROUPING" text="{i18n>masterGroupNoGrouping}" />
				<core:Item key="CompanyName" text="{i18n>masterGroupCustomer}" />
				<core:Item key="OrderPeriod" text="{i18n>masterGroupOrderPeriod}" />
				<core:Item key="ShippedPeriod" text="{i18n>masterGroupShippedPeriod}" />
			</semantic:GroupSelect>
		</semantic:group>

	</semantic:MasterPage>

</mvc:View>